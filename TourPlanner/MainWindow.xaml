<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Name="FileButton" Content="File" Click="FileButton_Click">
                <Button.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="New"/>
                        <MenuItem Header="Open"/>
                        <Separator/>
                        <MenuItem Header="Exit" Click="Exit_Click"/>
                    </ContextMenu>
                </Button.ContextMenu>
            </Button>
            <Button Grid.Column="1" Content="Edit" />
            <Button Grid.Column="2" Content="Options" />
            <Button Grid.Column="3" Content="Help" />
        </Grid>

        <Grid Grid.Row="1">
            <TextBox HorizontalAlignment="Stretch" Margin="5">Search...</TextBox>
        </Grid>

        <Grid Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <ListView ItemsSource="{Binding Tours}" Margin="5">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Tours" Width="200" DisplayMemberBinding="{Binding}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- GridSplitter for resizing -->
                <GridSplitter Grid.Column="1"
                              Width="5"
                              Margin="5"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              Background="Gray"/>
    
                <!-- Right Column -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Top Column -->
                    <TabControl Grid.Row="0">
                        <TabItem Header="General">
                            <StackPanel>
                                <TextBlock>General Placeholder</TextBlock>
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Route">
                            <StackPanel>
                                <TextBlock>Placeholder for Map</TextBlock>
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Misc">
                            <StackPanel>
                                <TextBlock>Placeholder for Misc.</TextBlock>
                            </StackPanel>
                        </TabItem>
                    </TabControl>

                    <!-- Grid Splitter for resizing -->
                    <GridSplitter Grid.Row="1"
                                  Height="5"
                                  Margin="5"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  ResizeDirection="Rows"
                                  Background="Gray"/>

                    <!-- Bottom Column -->
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Path=Logs}" AutoGenerateColumns="True" 
                              ColumnWidth="*" Margin="10" />
                    

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
