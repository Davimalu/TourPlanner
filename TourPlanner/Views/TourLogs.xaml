<UserControl x:Class="TourPlanner.Views.TourLogs"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.ViewModels"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <ui:DataGrid Grid.Row="0"
                  ItemsSource="{Binding SelectedTour.Logs}"
                  SelectedItem="{Binding SelectedLog, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="10">
            
            <ui:DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp"
                                    Binding="{Binding TimeStamp, StringFormat='yyyy-MM-dd HH:mm'}"
                                    Width="Auto"/>

                <DataGridTextColumn Header="Difficulty"
                                    Binding="{Binding Difficulty}"
                                    Width="SizeToHeader"/>

                <DataGridTextColumn Header="Distance (km)"
                                    Binding="{Binding DistanceTraveled, StringFormat={}{0} km}"
                                    Width="Auto"/>

                <DataGridTextColumn Header="Time (hrs)"
                                    Binding="{Binding TimeTaken, StringFormat={}{0} h}"
                                    Width="Auto"/>

                <DataGridTextColumn Header="Rating"
                                    Binding="{Binding Rating, StringFormat='N1'}"
                                    Width="SizeToHeader"/>

                <DataGridTextColumn Header="Comment"
                                    Binding="{Binding Comment}"
                                    Width="*"/> 
            </ui:DataGrid.Columns>
        </ui:DataGrid>

        <Grid Grid.Row="1" Margin="10 0 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <!-- TextBox nimmt den restlichen Platz ein -->
                <ColumnDefinition Width="Auto"/>
                <!-- Buttons -->
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Margin="5" Text="{Binding Path=NewLogName, UpdateSourceTrigger=PropertyChanged}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Margin="5" Content="Add" Command="{Binding Path=ExecuteAddNewTourLog}" MinWidth="60"/>
                <Button Margin="5" Content="Edit" Command="{Binding Path=ExecuteEditTourLog}" MinWidth="60"/>
                <Button Margin="5" Content="Delete" Command="{Binding Path=ExecuteDeleteTourLog}" MinWidth="60"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>