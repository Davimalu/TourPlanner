<UserControl x:Class="TourPlanner.Views.TourAttributes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border BorderThickness="1" BorderBrush="Gray" CornerRadius="8" Padding="10" Background="White">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>  <!-- Title -->
                        <RowDefinition Height="*"/>     <!-- Content area -->
                        <RowDefinition Height="Auto"/>  <!-- Button -->
                    </Grid.RowDefinitions>
                    
                    <ui:TextBlock  FontSize="24" Foreground="Black" FontWeight="Bold" Text="{Binding SelectedTour.TourName, TargetNullValue=''}" Margin="0,0,0,10"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Labels -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                            <ui:SymbolIcon Margin="0,0,6,0" Symbol="StarEmphasis24" />
                            <ui:TextBlock Text="Popularity: " FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0 0 10 0">
                            <ui:SymbolIcon Margin="0,0,6,0" Symbol="PersonHeart24" />
                            <ui:TextBlock Text="Child-Friendliness:" FontWeight="Bold" />
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                            <ui:SymbolIcon Margin="0,0,6,0" Symbol="BrainCircuit24" /> <!-- MyLocation24 -->
                            <ui:TextBlock Text="AI Summary:" FontWeight="Bold" />
                        </StackPanel>

                        <!-- Data Binding -->
                        <ProgressBar Grid.Row="0" Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding SelectedTour.Popularity, UpdateSourceTrigger=PropertyChanged}"/>
                        <ProgressBar Grid.Row="1" Grid.Column="1" Minimum="0" Maximum="100" Value="{Binding SelectedTour.ChildFriendlyRating, UpdateSourceTrigger=PropertyChanged}"/>
                        <ui:TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedTour.AiSummary, TargetNullValue=''}" TextWrapping="Wrap"/>
                    </Grid>
                    
                    <Grid Grid.Row="2">
                        <Button Content="Generate" HorizontalAlignment="Right" Margin="5" 
                                Command="{Binding ExecuteCalculateAttributes}"/>
                    </Grid>
                </Grid>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
