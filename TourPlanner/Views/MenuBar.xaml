<UserControl x:Class="TourPlanner.Views.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Menu Grid.Column="0" FontSize="14">
            <!-- File Menu -->
            <ui:MenuItem Header="File" Icon="{ui:SymbolIcon DocumentSplitHint24}">
                <MenuItem Header="Export" Command="{Binding ExecuteExportTours}" />
                <MenuItem Header="Import" Command="{Binding ExecuteImportTours}"/>
                <Separator/>
                <MenuItem Header="Exit" Command="{Binding Path=ExecuteExitApplication}" />
            </ui:MenuItem>
            <!-- Edit Menu -->
            <ui:MenuItem Header="Edit" Icon="{ui:SymbolIcon DocumentEdit24}" />
            <!-- Options Menu -->
            <ui:MenuItem Header="Options" Icon="{ui:SymbolIcon Settings24}" />
            <!-- Help Menu -->
            <ui:MenuItem Header="Help" Icon="{ui:SymbolIcon ChatHelp24}" />
        </Menu>
        
        <ui:ToggleSwitch Grid.Column="1"
                         x:Name="ThemeToggleSwitch"
                         OffContent="Light"
                         OnContent="Dark" 
                         HorizontalAlignment="Right"
                         VerticalAlignment="Center"  
                         Margin="0,0,10,0"
                         Click="ThemeToggleSwitch_Click">
        </ui:ToggleSwitch>
    </Grid>
</UserControl>
