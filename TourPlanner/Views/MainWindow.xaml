<Window x:Class="TourPlanner.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.Views"
        xmlns:viewModel="clr-namespace:TourPlanner.ViewModels"
        mc:Ignorable="d"
        Title="Tour Planner" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Grid Grid.Row="0">
            <local:MenuBar x:Name="MenuBar"/>
        </Grid>

        <!-- Search Bar -->
        <Grid Grid.Row="1">
            <local:SearchBar x:Name="SearchBar"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column -->
                <ListView ItemsSource="{Binding Tours}" Margin="5">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Tours" Width="200" DisplayMemberBinding="{Binding}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- GridSplitter for resizing -->
                <GridSplitter Grid.Column="1"
                              Width="5"
                              Margin="0 5 0 5"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Stretch"
                              Background="Gray"/>
    
                <!-- Right Column -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Top Column -->
                    <TabControl Grid.Row="0" Margin="5">
                        <TabItem Header="General">
                            <StackPanel>
                                <TextBlock>General Placeholder</TextBlock>
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Route">
                            <StackPanel>
                                <TextBlock>Placeholder for Map</TextBlock>
                            </StackPanel>
                        </TabItem>
                        <TabItem Header="Misc">
                            <StackPanel>
                                <TextBlock>Placeholder for Misc.</TextBlock>
                            </StackPanel>
                        </TabItem>
                    </TabControl>

                    <!-- Grid Splitter for resizing -->
                    <GridSplitter Grid.Row="1"
                                  Height="5"
                                  Margin="5 0 5 0"
                                  HorizontalAlignment="Stretch"
                                  VerticalAlignment="Center"
                                  ResizeDirection="Rows"
                                  Background="Gray"/>

                    <!-- Bottom Column -->
                    <DataGrid Grid.Row="2" ItemsSource="{Binding Path=Logs}" AutoGenerateColumns="True" 
                              ColumnWidth="*" Margin="5" />
                    

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
