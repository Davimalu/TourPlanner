<UserControl x:Class="TourPlanner.Views.TourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.ViewModels"
             xmlns:models="clr-namespace:TourPlanner.Models"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:ListView Grid.Row="0" ItemsSource="{Binding Tours}" SelectedItem="{Binding SelectedTour, Mode=TwoWay}" Margin="10">
            <ui:ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edit"
                              Command="{Binding ExecuteEditTour}"/>
                    <MenuItem Header="Delete"
                              Command="{Binding ExecuteDeleteTour}"/>
                </ContextMenu>
            </ui:ListView.ContextMenu>
            <ui:ListView.ItemTemplate >
                <DataTemplate DataType="{x:Type models:Tour}">
                    <TextBlock Margin="8,4" Text="{Binding TourName}"/>
                </DataTemplate>
            </ui:ListView.ItemTemplate>
        </ui:ListView>

        <Grid Grid.Row="1" Margin="10 0 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <ui:TextBox Grid.Column="0" Margin="5" Text="{Binding Path=NewTourName, UpdateSourceTrigger=PropertyChanged}"/>
            <ui:Button Grid.Column="1" Margin="5" Content="Add" Command="{Binding Path=ExecuteAddNewTour}" MinWidth="60"/>
        </Grid>
    </Grid>
</UserControl>
