<UserControl x:Class="TourPlanner.Views.TourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0" ItemsSource="{Binding Tours}" SelectedItem="{Binding SelectedTour, Mode=TwoWay}" Margin="10" Background="White">
            <ListView.ItemTemplate >
                <DataTemplate>
                    <Border Background="#f0f0f0" CornerRadius="5" Padding="8" Margin="5">
                        <TextBlock Text="{Binding TourName}" FontSize="14" Foreground="Black"/>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="1" Margin="10 0 10 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Margin="5" Text="{Binding Path=NewTourName, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1" Margin="5" Content="Add" Command="{Binding Path=ExecuteAddNewTour}" MinWidth="60"/>
            <Button Grid.Column="2" Margin="5" Content="Edit" Command="{Binding Path=ExecuteEditTour}" MinWidth="60"/>
            <Button Grid.Column="3" Margin="5" Content="Delete" Command="{Binding Path=ExecuteDeleteTour}" MinWidth="60"/>
        </Grid>
    </Grid>
</UserControl>
